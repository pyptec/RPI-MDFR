medidores:  
  ct01co2_sensor:
    device_name: "CT01CO2"
    id_device: 22
    port: "/dev/ttyS0"
    debug: false
    slave_id: 1
    baudrate: 9600
    bytesize: 8
    parity: N
    stopbits: 1
    timeout: 1
    
    simular: false  # <--- NUEVO FLAG para activar la simulación

    registers:
      - name: co2
        address: 3
        fc: 3
        decimals: 0        # decimals = 0 → deja el valor tal cual (entero), decimals = 1 → divide el valor por 10, decimals = 2 → divide por 100
        readWrite: "R"
        scale: 1
        unit: 139
        alias: co2
    control:
      co2_ppm_low: 3000     # por debajo o igual a este valor → GAS ON, EXTRACTOR OFF
      co2_ppm_high: 9000    # por encima o igual a este valor → GAS OFF, EXTRACTOR ON 
      aire_fresco_minutos: 12 # minutos que se activa el aire fresco cuando el CO2 supera el umbral alto
      